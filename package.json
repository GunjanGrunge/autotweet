{
  "name": "auto-tweet",
  "version": "1.0.0",
  "description": "Automated Twitter posting with OpenAI",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "twitter-api-v2": "^1.15.0",
    "openai": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "zip -r function.zip . -x '*.git*'"
  }
}
